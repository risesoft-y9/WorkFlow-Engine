spring:
    application:
        name: flowableUI
    profiles:
        active: test
    cloud:
        nacos:
            config:
                enabled: true
                namespace: is
                group: DEFAULT_GROUP
                server-addr: https://vue.youshengyun.com:443
                username: nacos
                password: Risesoft@2022
    config:
        import:
            - optional:nacos:flowableUI.yml